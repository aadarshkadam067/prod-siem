# ============================================================
# prod-siem — Python dependencies
# ============================================================
# All versions pinned for reproducibility.
# Install:   pip install -r requirements.txt
# Tested on: Python 3.11, 3.12, 3.13
# ============================================================


# ---------- Core: ASGI web framework ----------
fastapi==0.115.0
uvicorn[standard]==0.32.0      # ASGI server; [standard] adds websockets + httptools
pydantic==2.10.3
pydantic-settings==2.6.1       # typed env-var loading


# ---------- Storage & cache ----------
elasticsearch==8.15.0           # async client used in main.py lifespan
aiohttp==3.10.11                # REQUIRED transport for AsyncElasticsearch
                                # (elasticsearch lib loads it at runtime — never imported directly)
redis==5.2.0                    # incl. redis.asyncio for async pub/sub


# ---------- HTTP clients ----------
httpx==0.27.2                   # async HTTP — used by integrations/ (TheHive, Cortex, MISP)
requests==2.32.3                # sync HTTP — used in scripts/ and a few sync paths


# ---------- AI / LLM ----------
groq==0.11.0                    # Groq SDK — LLaMA 3.3 70B for alert triage


# ---------- Configuration & utilities ----------
python-dotenv==1.0.1            # .env loading
PyYAML==6.0.2                   # config/rule loading from configs/ + sigma_rules/


# ---------- Reporting ----------
reportlab==4.2.5                # PDF incident report generator (incident_response/)


# ---------- Logging ----------
loguru==0.7.2                   # structured logger used throughout backend


# ============================================================
# Dev / test (install separately:  pip install -r requirements-dev.txt)
# ============================================================
# pytest==8.3.3
# pytest-asyncio==0.24.0
# ruff==0.7.4
